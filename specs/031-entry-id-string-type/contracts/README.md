# Contracts

No API contract changes for this feature. It is a client-side TypeScript type-correctness
refactor: `GET /api/entries` and `GET /api/attachment-analyses` payloads are unchanged (both
already return the entry id / `entryId` as a string). No new or modified endpoints.
